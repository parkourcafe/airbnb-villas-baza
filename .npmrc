# Fail fast when the wrong Node/pnpm is used (spec pins Node 24 LTS + pnpm).
engine-strict=true
# Keep the dependency tree strict and deterministic.
auto-install-peers=true
strict-peer-dependencies=false
# Reproducible installs.
prefer-frozen-lockfile=true
save-exact=true
