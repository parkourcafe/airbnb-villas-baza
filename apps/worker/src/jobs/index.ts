/**
 * Job handlers (import, snapshot, event, export, maintenance) live here.
 * Implemented from milestone 3 onward once the Postgres job queue exists.
 */
export const JOB_HANDLERS_PLACEHOLDER = true;
