/**
 * Runners orchestrate a claimed job through the engines (validate -> normalize
 * -> snapshot -> diff -> event). Implemented from milestone 3 onward.
 */
export const RUNNERS_PLACEHOLDER = true;
