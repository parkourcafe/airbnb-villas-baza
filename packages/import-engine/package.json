{
  "name": "@bai/import-engine",
  "version": "0.0.0",
  "private": true,
  "description": "CSV validation, header mapping, row parsing and import idempotency. Milestone 0 ships the CSV header contract only.",
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "clean": "rm -rf .turbo coverage *.tsbuildinfo"
  },
  "dependencies": {
    "@bai/domain": "workspace:*",
    "csv-parse": "7.0.1"
  },
  "devDependencies": {
    "@bai/config": "workspace:*",
    "@bai/test-fixtures": "workspace:*",
    "@types/node": "22.20.1",
    "eslint": "9.39.5",
    "typescript": "5.9.3",
    "vitest": "3.2.7"
  }
}
