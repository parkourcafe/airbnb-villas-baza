/**
 * `@bai/snapshot-engine` - immutable snapshots and fingerprints.
 *
 * Milestone 0 provides the deterministic normalization/hashing primitives that
 * the full engine (milestone 4) builds on. Snapshot insertion, comparability
 * rules and diffing are NOT implemented yet.
 */
export * from "./normalize";
